{
  "name": "project-auth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch database.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^6.0.0"
  }
}
