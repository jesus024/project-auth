{
  "name": "petcare-center-spa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch database.json --port 3001"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "json-server": "^0.17.4"
  }
}
